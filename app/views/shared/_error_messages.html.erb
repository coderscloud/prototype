<% if @user.errors.any? %>
  <div id="error_explanation">
    <p>Error saving the user in the following field(s):</p>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>