
module UsersHelper
    def gravatar_for(user, options = { :size => 50 })
      gravatar_image_tag(user.email.downcase, :alt => user.login,
                                              :class => 'gravatar',
                                              :gravatar => {:default => '#{root_url}/images/profile_photo.jpg'})
    end
end
